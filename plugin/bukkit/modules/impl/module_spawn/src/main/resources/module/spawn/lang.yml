# 配置文件版本号(请勿修改)
config_version: "${project.version}"

commands:
  spawn:
    usage: "/{command}"
    description: "返回出生点"
    message: "{prefix} <#EAE4FF>你回到了出生点!"
  setspawn:
    usage: "/{command}"
    description: "设置出生点"
    message: "{prefix} <#EAE4FF>你成功将此处设置为出生点!"
