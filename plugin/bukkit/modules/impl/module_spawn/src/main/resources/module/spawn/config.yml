# 配置文件版本号(请勿修改)
config_version: "${project.version}"

# 出生点
#
# 开关
enable: true

# 自动传送
auto_teleport:
  # 复活
  respawn: false
  # 加入服务器
  join: false

# 命令
spawn_commands:
  - "spawn"

setspawn_commands:
  - "setspawn"

# 黑名单世界
black_world:
  - "pvp"
