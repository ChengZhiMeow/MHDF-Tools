# 配置文件版本号(请勿修改)
config_version: "${project.version}"

# 击退玩家
#
# 开关
enable: true
# 默认击退模式
# (normal, random)
default_type: "normal"
# 击退力度
vector:
  x: 0.75
  y: 0.5
  z: 0.75
# 命令
commands:
  - "kb"
  - "knockback"