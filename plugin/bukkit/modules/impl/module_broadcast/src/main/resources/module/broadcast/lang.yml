# 配置文件版本号(请勿修改)
config_version: "${project.version}"

commands:
  broadcast:
    usage: "/{command} [消息内容]"
    description: "全服消息通知"
    format: "{prefix} {message}"
    message: "{prefix} <#EAE4FF>发送成功!"
