# 配置文件版本号(请勿修改)
config_version: "${project.version}"

# 标题
title: "<#EAE4FF>示例菜单"

# 格子布局
slots:
  - "XXXXXXXXX"
  - "XXXXIXXXX"
  - "XXXXXXXXX"

# 直接打开该菜单的命令
commands:
  - "example"

# 打开菜单时执行的操作
open_actions: [ ]

# 关闭菜单时执行的操作
close_actions: [ ]

# 物品
items:
  X:
    by: "Vanilla"
    type: "gray_stained_glass_pane"
    name: "&f "
  I:
    by: "Vanilla"
    type: "paper"
    name: "<#EAE4FF>示例物品"
    lore:
      - "&7点击后发送一条消息"
    actions:
      click:
        - actions:
            - type: "message"
              message: "&b你点击了示例物品!"
