# 配置文件版本号(请勿修改)
config_version: "${project.version}"

# 请求传送到指定玩家位置
#
# 开关
enable: true

# 请求冷却时间(单位: 秒, 可设置为0关闭冷却)
delay: 60

# 请求超时时间(单位: 秒, 最小为1)
time: 60

# 命令
commands:
  - "tpa"

# 黑名单世界
black_world:
  - "pvp"
