# 配置文件版本号(请勿修改)
config_version: "${project.version}"

# 缓存
#
# 缓存类型
# (map redis)
type: "map"
# 服务池ID
server: "default"

# Redis数据库
redis:
  # 地址
  host: "localhost:6379"
  # 用户
  user: ""
  # 密码
  password: ""
