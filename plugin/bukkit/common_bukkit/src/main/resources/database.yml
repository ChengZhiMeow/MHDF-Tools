# 配置文件版本号(请勿修改)
config_version: "${project.version}"

# 数据库类型
# (mysql h2)
type: "h2"

# MySQL数据库
mysql:
  # 地址
  host: "127.0.0.1:3306"
  # 数据库
  database: "mhdf_tools"
  # 用户
  user: "root"
  # 密码
  password: "root"

# H2数据库
h2:
  # 数据库文件
  file: "database.db"

# 自定义连接参数
prams: { }
